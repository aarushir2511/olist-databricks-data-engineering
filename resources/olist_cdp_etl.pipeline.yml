resources:
  pipelines:
    olist_cdp_etl:
      name: Olist_CDP_ETL
      catalog: ${var.catalog}
      schema: bronze
      serverless: true
      photon: true
      continuous: false
      development: false
      channel: CURRENT

      libraries:
        - glob:
            include: ../src/transformations/**/*.sql