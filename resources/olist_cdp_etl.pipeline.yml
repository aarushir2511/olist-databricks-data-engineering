resources:
  pipelines:
    olist_cdp_etl:
      name: Olist_CDP_ETL
      catalog: ${var.catalog}
      schema: bronze
      serverless: true
      photon: true
      continuous: false
      development: false
      channel: CURRENT

      configuration:
        volume_path: "${var.volume_path}"

      libraries:
        - glob:
            include: ../src/transformations/**