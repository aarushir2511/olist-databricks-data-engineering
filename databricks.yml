# Declarative Automation Bundle configuration, more info here: https://docs.databricks.com/en/dev-tools/bundles/settings.html
bundle:
  name: "olist-cdp-etl-bundle"

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  # The 'dev' target, for development purposes. This target is the default.
  dev:
    mode: development
    default: true
    workspace:
      host: https://dbc-ad77934a-6f2e.cloud.databricks.com

  # The 'prod' target, used for production deployment.
  prod:
    mode: production
    workspace:
      host: https://dbc-ad77934a-6f2e.cloud.databricks.com
      # We always use /Users/aarushiranjan0@gmail.com for all resources to make sure we only have a single copy.
      root_path: /Users/aarushiranjan0@gmail.com/.bundle/${bundle.name}/${bundle.target}
    run_as:
      service_principal_name: "f8e6bd5c-f541-47d0-92c8-13aa54154f07"
